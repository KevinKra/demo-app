import { createTheme } from "@mui/material/styles";

export const THEME_LIGHT = createTheme({
  status: {
    danger: "#555",
  },
  palette: {
    brands: {
      github: "#151413",
      google: "#4285f4",
    },
  },
});
