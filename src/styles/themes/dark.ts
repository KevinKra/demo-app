import { createTheme } from "@mui/material/styles";

export const THEME_DARK = createTheme({
  status: {
    danger: "#555",
  },
  palette: {
    // primary?: '#000',
    // secondary?: "123456",
    brands: {
      github: "#151413",
      google: "#4285f4",
    },
  },
});
